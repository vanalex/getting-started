greeting:
  message: hello
  name: quarkus

server:
  host: localhost
  port: 8080
  log:
    enabled: true
    suffix: .log
    rotate: false

quarkus:
  mongodb:
    connection-string: mongodb://localhost:27017

quarkus:
  mongodb:
    devservices:
      enabled: true